streamlit>=1.32
# tensorflow-cpu: smaller than full tensorflow (no CUDA wheels) so it fits
# Streamlit Community Cloud resource limits. Capped <2.20 because newer
# builds only ship wheels for Python >=3.13 and Cloud's default is 3.12.
# NOTE: requires Python <=3.12 on the deploy target (see runtime.txt).
tensorflow-cpu>=2.16,<2.20
numpy<2.2
pillow
matplotlib
plotly>=5.18
pandas
